eval "$(starship init zsh)"
eval "$(zoxide init zsh)"
export PATH="$HOME/.local/bin:$PATH"
export PATH="$HOME/apps/nvim-macos-arm64/bin:$PATH"
export PATH="$HOME/apps/gh/bin:$PATH"

#alias
alias code="nvim"
alias reframe="yabai --restart-service"
alias cuda_machine="ssh 192.168.40.28"
alias reconfigure="source ~/.zshrc"

#funcions
xbs() {
  [ -z "$1" ] && { echo "usage: xbs <name>"; return 1; }
  xcode-build-server config -project "$1.xcodeproj" -scheme "$1"
}

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# bun completions
[ -s "/Users/a3dif3x/.bun/_bun" ] && source "/Users/a3dif3x/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# opencode
export PATH=/Users/skap3r/.opencode/bin:$PATH
